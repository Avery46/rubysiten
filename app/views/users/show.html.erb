<h1 class="text-left mt-4 mx-5"><%= @user.username %></h1>

<% if logged_in? && @user == current_user %>
  <div class="text-left mt-4 mx-5 mb-5">
    <%= link_to "Редактирование профиля", edit_user_path(@user), class: "btn btn-outline-danger" %>
  </div>
<% end %>

<h3 class="text-left mt-5 mx-5">Статьи</h3>
<div class="row">
<div class="col-0">

</div>
<div class="col-12">
<div class="flickr_pagination">                        
   <%= will_paginate @lists, :container => false %>   
 </div>  

  <%= render 'lists/list' %>

 <div class="flickr_pagination mb-4">               
   <%= will_paginate @lists, :container => false %>
 </div> 
</div>

</div>
 

